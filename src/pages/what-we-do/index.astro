---
import Base from '../../layouts/Base.astro';
import PageHeader from '../../components/layout/PageHeader.astro';
import Services from '../../components/sections/Services.astro';
import CtaBand from '../../components/sections/CtaBand.astro';
---

<Base
  title="What we do — MindStack"
  description="Three services that hand off to each other, not to someone else: web platforms, product and app engineering, cloud and infrastructure."
>
  <PageHeader
    eyebrow="What we do"
    title="Three services that hand off to each other, not to someone else."
    lead="The differentiator available to a firm that builds and then runs software is continuity — the same team that ships the app owns the infrastructure it runs on."
  />
  <Services />
  <CtaBand />
</Base>
